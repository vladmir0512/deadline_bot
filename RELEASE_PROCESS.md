# Процесс выпуска новых версий Deadline Bot

## 📋 Обзор

Этот документ описывает процесс управления релизами, версионирования и развертывания новых версий Deadline Bot.

## 🔀 Git Flow и ветки

### Основные ветки:
- **`main`** - Production-ready код, всегда стабильный
- **`develop`** - Основная ветка разработки, содержит последние изменения

### Вспомогательные ветки:
- **`feature/*`** - Новые функции (например: `feature/add-notifications`)
- **`hotfix/*`** - Критические исправления (например: `hotfix/fix-db-connection`)
- **`release/*`** - Подготовка к релизу (например: `release/v1.2.0`)

## 🏷️ Семантическое версионирование

Используем [Semantic Versioning](https://semver.org/):

```
MAJOR.MINOR.PATCH

Примеры:
- 1.0.0 - Первый релиз
- 1.0.1 - Исправление багов (patch)
- 1.1.0 - Новая функция (minor)
- 2.0.0 - Breaking changes (major)
```

### Когда увеличивать версию:

#### **PATCH (1.0.0 → 1.0.1)**
- Исправление багов
- Безопасные улучшения
- Рефакторинг без изменения API

#### **MINOR (1.0.0 → 1.1.0)**
- Новые функции (backward compatible)
- Улучшения существующего функционала
- Добавление опций/параметров

#### **MAJOR (1.0.0 → 2.0.0)**
- Breaking changes
- Удаление функций
- Значительные изменения API

## 🚀 Процесс выпуска релиза

### ШАГ 1: Подготовка к релизу

#### 1.1 Автоматическая подготовка релиза (рекомендуется)
```bash
# Используйте скрипт для автоматизации
python scripts/create_release.py

# Скрипт автоматически:
# - Определит новую версию
# - Сгенерирует changelog из коммитов
# - Создаст ветку релиза
# - Обновит файлы version.py и CHANGELOG.md
# - Закоммитит и отправит изменения
```

#### 1.2 Ручная подготовка релиза
```bash
# Из develop ветки
git checkout develop
git pull origin develop

# Создайте ветку релиза
git checkout -b release/v1.2.0
```

#### 1.2 Обновите версию в коде
```python
# В файле healthcheck.py или другом месте где указана версия
__version__ = "1.2.0"
```

#### 1.3 Обновите CHANGELOG.md
```markdown
# Changelog

## [1.2.0] - 2025-12-16

### Added
- Добавлена автоматическая синхронизация перед /my_deadlines
- Улучшен health check с отдельным сервером

### Fixed
- Исправлено подключение к БД в Docker
- Улучшена обработка переменных окружения

### Changed
- Обновлена документация по деплою
```

#### 1.4 Тестирование
```bash
# Запустите все тесты
python -m scripts.verify_all

# Протестируйте Docker
docker-compose build
docker-compose up -d
curl http://localhost:8080/health

# Ручное тестирование функций
```

### ШАГ 2: Merge в main

#### 2.1 Создайте Pull Request
```bash
# Отправьте ветку на GitHub
git push origin release/v1.2.0

# Создайте PR из release/v1.2.0 в main
# Название: "Release v1.2.0"
# Описание: изменения из CHANGELOG.md
```

#### 2.2 Code Review и тестирование
- [ ] Код прошел ревью
- [ ] Все тесты проходят (CI/CD)
- [ ] Docker образ собирается
- [ ] Health check работает
- [ ] Ручное тестирование в staging

#### 2.3 Merge в main
```bash
# После аппрува PR
git checkout main
git merge release/v1.2.0
git push origin main
```

### ШАГ 3: Создание GitHub Release

#### 3.1 Создайте тег
```bash
# Создайте аннотированный тег
git tag -a v1.2.0 -m "Release version 1.2.0

- Добавлена автоматическая синхронизация перед /my_deadlines
- Улучшен health check с отдельным сервером
- Исправлено подключение к БД в Docker"

# Отправьте тег
git push origin v1.2.0
```

#### 3.2 Создайте Release на GitHub
1. Перейдите на https://github.com/your-username/deadline_bot/releases
2. Нажмите "Create a new release"
3. **Tag version**: `v1.2.0`
4. **Target**: `main`
5. **Release title**: `Release v1.2.0`
6. **Description**: Скопируйте из CHANGELOG.md
7. **Mark as pre-release**: Нет (для стабильных релизов)
8. Нажмите "Publish release"

### ШАГ 4: Автоматический деплой

#### 4.1 CI/CD Pipeline
После создания релиза автоматически запустится:
- Сборка Docker образа
- Push в Docker Hub с тегом `v1.2.0` и `latest`
- Обновление production (если настроено)

#### 4.2 Ручной деплой (если не настроен автоматический)
```bash
# На production сервере
cd /opt/deadline-bot

# Обновите код
git pull origin main

# Пересоберите и перезапустите
docker-compose build --no-cache
docker-compose up -d

# Проверьте
curl http://localhost:8080/health
```

### ШАГ 5: Merge обратно в develop

```bash
# Вернитесь к develop
git checkout develop
git merge main
git push origin develop

# Удалите ветку релиза
git branch -d release/v1.2.0
git push origin --delete release/v1.2.0
```

## 🔧 Hotfix процесс

### Для критических багов в production:

#### 1. Создайте hotfix ветку
```bash
git checkout main
git checkout -b hotfix/critical-db-fix
```

#### 2. Исправьте баг
```bash
# Внесите изменения
git commit -m "fix: critical database connection issue"
```

#### 3. Merge в main и develop
```bash
git checkout main
git merge hotfix/critical-db-fix
git push origin main

git checkout develop
git merge main
git push origin develop
```

#### 4. Создайте patch релиз
```bash
git tag -a v1.2.1 -m "Hotfix: critical database fix"
git push origin v1.2.1

# Создайте GitHub release как описано выше
```

## 📊 Мониторинг после релиза

### Немедленная проверка:
```bash
# Health check
curl http://your-server:8080/health

# Логи на ошибки
docker-compose logs --since="1 hour ago" | grep -i error

# Тестирование функций
# Напишите боту в Telegram
```

### Мониторинг в первые часы:
- Количество пользователей
- Время отклика бота
- Ошибки в логах
- Использование ресурсов (CPU, RAM)

## 🔄 Rollback стратегия

### Если релиз не удался:

#### Вариант 1: Rollback через Git
```bash
# На сервере
cd /opt/deadline-bot

# Вернитесь к предыдущему релизу
git checkout v1.1.0  # или предыдущий тег

# Перезапустите
docker-compose build --no-cache
docker-compose up -d
```

#### Вариант 2: Rollback через Docker
```bash
# Запустите старую версию образа
docker-compose pull  # если образы в registry
# или
docker tag deadline-bot:v1.1.0 deadline-bot:latest
docker-compose up -d
```

## 📅 Release Schedule

### Рекомендуемый ритм:
- **Minor releases**: Каждые 2-4 недели
- **Patch releases**: По мере необходимости (критические баги)
- **Major releases**: При значительных изменениях (1-2 раза в год)

### Release branches:
- Держите ветки релиза открытыми не дольше 1-2 дней
- Удаляйте merged ветки сразу после релиза

## 🛠️ Инструменты и автоматизация

### GitHub Actions для релизов:
```yaml
# В .github/workflows/release.yml
name: Release
on:
  release:
    types: [published]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to production
        run: |
          echo "Deploy version ${{ github.event.release.tag_name }}"
          # Ваш скрипт деплоя
```

### Автоматическое версионирование:
```bash
# Используйте semantic-release или similar tools
npm install -g semantic-release
semantic-release
```

## 📋 Checklist перед релизом

### Код:
- [ ] Все тесты проходят
- [ ] Код прошел ревью
- [ ] Линтинг без ошибок
- [ ] Docker образ собирается

### Документация:
- [ ] CHANGELOG.md обновлен
- [ ] README.md актуален
- [ ] Версия в коде обновлена

### Тестирование:
- [ ] Ручное тестирование всех функций
- [ ] Health check работает
- [ ] Логи чистые
- [ ] Производительность OK

### Деплой:
- [ ] CI/CD настроен
- [ ] Резервные копии сделаны
- [ ] Rollback план готов

## 🎯 Резюме

**Процесс релиза:**
1. `develop` → `release/v1.2.0` → тестирование
2. `release/v1.2.0` → `main` → тег `v1.2.0`
3. GitHub Release → автоматический деплой
4. `main` → `develop` → удаление ветки

**Время на релиз:** 1-2 дня (включая тестирование)

**Частота:** 2-4 недели для minor releases

Этот процесс обеспечивает стабильность production и возможность быстрого отката при проблемах.
